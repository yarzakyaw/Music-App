plugins {
    id "com.android.application"
    id 'com.google.gms.google-services'
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

dependencies {
  // implementation("com.google.firebase:firebase-appcheck-debug") // remove in production
  implementation("com.google.firebase:firebase-appcheck-playintegrity")
}

android {
    namespace = "com.example.mingalar_music_app"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17
    }

    defaultConfig {
        applicationId = "com.mingalar.music"
        minSdk = 23
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    signingConfigs {  
        release {  
            keyAlias 'mingalarmusic'
            keyPassword '3775110445'
            storeFile file('mykey/mingalarmusickey.jks')
            storePassword '3775110445'
        }  
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            // signingConfig = signingConfigs.debug
            signingConfig signingConfigs.release
            // Optional: Enable ProGuard or R8 for code shrinking  
            minifyEnabled false // Should be false if you want to generate symbols without minification
            shrinkResources false
            // Enable debug symbols
            // ndk {  
            //     debugSymbolLevel 'FULL' // You can also use 'SYMBOL_TABLE'
            // }
        }
    }
}

flutter {
    source = "../.."
}
